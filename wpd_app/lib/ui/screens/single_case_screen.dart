import 'package:flutter/material.dart';

class SingleCaseScreen extends StatelessWidget {
  const SingleCaseScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Case Screen'),
      ),
    );
  }
}
